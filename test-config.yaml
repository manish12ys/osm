# Omarchy System Monitor - Test Configuration

# Theme
theme: "Dracula"

# Refresh rate in milliseconds (2000ms = 2 seconds for better performance)
refresh_rate: 2000

# Default page
default_page: "procs"

# Remote Monitoring (disabled for local testing)
remote_mode: false
remote_url: ""
remote_server: false
remote_port: "8080"

# Plugin System (enabled for testing)
plugins_enabled: true
